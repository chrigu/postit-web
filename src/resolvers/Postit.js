export const Postit = {
    id: postit => postit.id,
    url: postit => postit.url,
    detectedText: postit => postit.detectedText,
    x: postit => postit.x,
    y: postit => postit.y,
    width: postit => postit.width,
    height: postit => postit.height,
    image: postit => postit.image
}